# Host port exposed by docker compose.
GIP_HTTP_PORT=8080

# Number of image generation jobs the backend worker may run at the same time.
GIP_GENERATION_CONCURRENCY=1

# Default timeout for custom async provider polling, in seconds.
GIP_CUSTOM_POLL_TIMEOUT_SECONDS=900
